export type HTTPuppyWriterOptions = {
	status		: number;
	statusText	: string;
	type		: string;
	asString	: string;
	asBuffer	: Buffer;
}
export type HTTPHeader = string[];
export type HTTPHeaders = HTTPHeader[];
